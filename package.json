{
  "name": "lazycache",
  "version": "0.0.2",
  "description": "automatic reset cache asynchronously when expired",
  "main": "index.js",
  "scripts": {
    "test": "node node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/csbun/lazy-cache.git"
  },
  "keywords": [
    "cache",
    "async"
  ],
  "author": "Hans Chan",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/csbun/lazy-cache/issues"
  },
  "homepage": "https://github.com/csbun/lazy-cache",
  "dependencies": {
  },
  "devDependencies": {
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.17",
    "mocha": "^2.2.5",
    "mocha-lcov-reporter": "0.0.2"
  }
}
